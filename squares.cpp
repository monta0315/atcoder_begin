#include <bits/stdc++.h>
using namespace std;
#define rep(i, n) for (int i = 0; i < (n); i++)
#define all(v) sort(v.begin(), v.end())
typedef long long ll;
using Graph = vector<vector<int>>;
const int num = 1000000007;

int main(){
  int t;
  cin >> t;
  vector<long> n(t), a(t), b(t);
  rep(i,t){
    cin >> n.at(i) >> a.at(i) >> b.at(i);
  }
}
